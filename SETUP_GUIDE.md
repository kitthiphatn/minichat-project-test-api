# 🚀 คู่มือการติดตั้งและใช้งาน Mini Chat Ollama

## 📦 สิ่งที่ได้รับในโปรเจคนี้

โปรเจคนี้เป็นแอปพลิเคชันแชทกับ AI ที่สมบูรณ์ มี **30 ไฟล์** ประกอบด้วย:

- ✅ **Backend API** (Express.js + MongoDB) - 8 ไฟล์
- ✅ **Frontend Web** (Next.js 14 + Tailwind CSS) - 11 ไฟล์
- ✅ **เอกสารครบถ้วน** - 7 ไฟล์
- ✅ **ไฟล์ตั้งค่า** - 4 ไฟล์

**รองรับ AI 4 ตัว:**
1. **Ollama** - ฟรี 100%, รันบนเครื่องตัวเอง
2. **Groq** - ฟรี, ตอบเร็วมาก, ใช้ผ่านอินเทอร์เน็ต
3. **OpenRouter** - เสียเงิน, มี AI หลายรุ่น
4. **Anthropic** - เสียเงิน, Claude AI คุณภาพสูง

---

## ⚡ วิธีติดตั้งแบบเร็ว (5-10 นาที)

### ขั้นที่ 1: ติดตั้งโปรแกรมพื้นฐาน

1. **Node.js** (จำเป็น)
   - ดาวน์โหลด: https://nodejs.org/
   - เลือกเวอร์ชัน LTS
   - ติดตั้งตามขั้นตอน

2. **MongoDB** (จำเป็น - เลือกอย่างใดอย่างหนึ่ง)
   
   **ตัวเลือก A: MongoDB Compass (แนะนำ - ง่ายที่สุด)**
   - ดาวน์โหลด: https://www.mongodb.com/try/download/compass
   - ติดตั้งแล้ว MongoDB จะรันอัตโนมัติ
   
   **ตัวเลือก B: Docker**
   - ติดตั้ง Docker Desktop: https://www.docker.com/
   - เปิด Docker Desktop
   - ใน Command Prompt พิมพ์: `docker-compose up -d`

### ขั้นที่ 2: ติดตั้ง Dependencies

เปิด Command Prompt ในโฟลเดอร์โปรเจค แล้วพิมพ์:

```bash
# ติดตั้ง Backend
cd backend
npm install

# ติดตั้ง Frontend
cd ..\frontend
npm install
```

### ขั้นที่ 3: สร้างไฟล์ .env (Windows)

```bash
# สร้างไฟล์ .env สำหรับ Backend
cd ..\backend
copy .env.example .env

# สร้างไฟล์ .env.local สำหรับ Frontend
cd ..\frontend
copy .env.local.example .env.local
```

---

## 🎯 เลือกวิธีใช้ AI (เลือก 1 หรือ 2 วิธี)

### วิธีที่ 1: ใช้ Ollama (ฟรี 100% แต่ต้องติดตั้ง)

**ข้อดี:**
- ✅ ฟรีตลอดไป
- ✅ ไม่ต้องใช้อินเทอร์เน็ต (หลังดาวน์โหลด model)
- ✅ ข้อมูลปลอดภัย ไม่ออกจากเครื่อง

**ข้อเสีย:**
- ❌ ต้องดาวน์โหลด AI model (4-5 GB)
- ❌ ต้องมี RAM อย่างน้อย 8 GB
- ❌ ตอบช้ากว่าแบบคลาวด์

**วิธีติดตั้ง:**

1. ดาวน์โหลด Ollama: https://ollama.ai/download
2. ติดตั้งตามขั้นตอน
3. รีสตาร์ท Command Prompt
4. เปิด Command Prompt หน้าต่างที่ 1 พิมพ์:
   ```bash
   ollama serve
   ```
5. เปิด Command Prompt หน้าต่างที่ 2 พิมพ์:
   ```bash
   ollama pull llama3
   ```
   (รอดาวน์โหลด 4-5 GB)

---

### วิธีที่ 2: ใช้ Groq (ฟรี ไม่ต้องติดตั้งอะไร)

**ข้อดี:**
- ✅ ฟรี (มี limit)
- ✅ ไม่ต้องติดตั้งอะไร
- ✅ ตอบเร็วมาก (< 1 วินาที)
- ✅ ไม่กิน RAM

**ข้อเสีย:**
- ❌ ต้องมีอินเทอร์เน็ต
- ❌ มี rate limit (ฟรี 30 requests/นาที)

**วิธีตั้งค่า:**

1. **สมัคร Groq (ฟรี)**
   - ไปที่: https://console.groq.com/
   - คลิก "Sign Up" (ใช้ Google Account ได้)
   - ไปที่ API Keys
   - คลิก "Create API Key"
   - คัดลอก API Key (เริ่มต้นด้วย `gsk_`)

2. **ตั้งค่า API Key**
   - เปิดไฟล์ `backend\.env` ด้วย Notepad
   - หาบรรทัด `# GROQ_API_KEY=`
   - แก้เป็น: `GROQ_API_KEY=gsk_xxxxxxxxxxxxxxxx` (ใส่ API Key ของคุณ)
   - บันทึกไฟล์

3. **เสร็จแล้ว!** ไม่ต้องติดตั้งอะไรเพิ่ม

---

## 🚀 วิธีเปิดใช้งาน

### เปิดครั้งแรก (ทำครั้งเดียว)

1. **ตรวจสอบ MongoDB**
   - ถ้าใช้ MongoDB Compass: เปิดโปรแกรม MongoDB Compass
   - ถ้าใช้ Docker: `docker-compose up -d`

2. **เปิด Backend** (Command Prompt หน้าต่างที่ 1)
   ```bash
   cd backend
   npm run dev
   ```
   รอจนเห็น "Server running on http://localhost:5000"

3. **เปิด Frontend** (Command Prompt หน้าต่างที่ 2)
   ```bash
   cd frontend
   npm run dev
   ```
   รอจนเห็น "Ready on http://localhost:3000"

4. **เปิด Ollama** (ถ้าใช้ Ollama - Command Prompt หน้าต่างที่ 3)
   ```bash
   ollama serve
   ```

5. **เปิดเว็บเบราว์เซอร์**
   - ไปที่: http://localhost:3000
   - จะเห็นหน้าแชท!

---

## 💬 วิธีใช้งานหน้าแชท

1. **เลือก AI Provider**
   - ถ้าใช้ Ollama: เลือก "Ollama (Local)"
   - ถ้าใช้ Groq: เลือก "Groq"

2. **เลือก Model**
   - Ollama: เลือก "llama3"
   - Groq: เลือก "llama3-8b-8192" (เร็วที่สุด)

3. **พิมพ์ข้อความ**
   - พิมพ์คำถามในช่องด้านล่าง
   - กด Enter หรือคลิก "Send"

4. **รอ AI ตอบ**
   - Ollama: ประมาณ 5-10 วินาที
   - Groq: ประมาณ 1-2 วินาที

---

## 🔧 แก้ปัญหาที่พบบ่อย

### ปัญหา 1: Backend ไม่เริ่มทำงาน (Port 5000 ถูกใช้แล้ว)

**อาการ:** ขึ้น "EADDRINUSE" หรือ "Port 5000 is already in use"

**วิธีแก้:**
```bash
# Windows
netstat -ano | findstr :5000
# จดเลข PID (ตัวเลขท้ายสุด)

taskkill /PID <เลข PID> /F

# แล้วรัน Backend ใหม่
cd backend
npm run dev
```

### ปัญหา 2: MongoDB ไม่เชื่อมต่อ

**อาการ:** ขึ้น "MongoDB connection failed"

**วิธีแก้:**
- ถ้าใช้ Compass: เปิดโปรแกรม MongoDB Compass
- ถ้าใช้ Docker: `docker-compose up -d`
- ตรวจสอบว่า MongoDB รันที่ port 27017

### ปัญหา 3: Ollama ไม่ตอบ

**อาการ:** ขึ้น "Ollama connection failed"

**วิธีแก้:**
```bash
# ตรวจสอบว่า Ollama รันอยู่
ollama list

# ถ้าไม่รัน ให้เปิด
ollama serve

# ตรวจสอบว่ามี model
ollama list
# ถ้าไม่มี llama3 ให้ดาวน์โหลด
ollama pull llama3
```

### ปัญหา 4: Groq ไม่ทำงาน

**อาการ:** ขึ้น "Groq API key not configured" หรือ "401 Unauthorized"

**วิธีแก้:**
1. ตรวจสอบไฟล์ `backend\.env`
2. ต้องมีบรรทัด: `GROQ_API_KEY=gsk_xxxxxxxx`
3. ไม่มี `#` ข้างหน้า
4. รีสตาร์ท Backend (Ctrl+C แล้วรันใหม่)

### ปัญหา 5: Frontend ไม่เชื่อมต่อ Backend

**อาการ:** ขึ้น "Network Error" หรือ "Failed to fetch"

**วิธีแก้:**
1. ตรวจสอบว่า Backend รันอยู่ (http://localhost:5000/health)
2. ตรวจสอบไฟล์ `frontend\.env.local`
3. ต้องมี: `NEXT_PUBLIC_API_URL=http://localhost:5000/api`
4. รีสตาร์ท Frontend

---

## 📋 Checklist ก่อนส่งโปรเจค

- [ ] ติดตั้ง dependencies ครบ (backend + frontend)
- [ ] มีไฟล์ `.env` และ `.env.local`
- [ ] Backend รันได้ (http://localhost:5000/health แสดง OK)
- [ ] Frontend รันได้ (http://localhost:3000 เปิดได้)
- [ ] MongoDB เชื่อมต่อสำเร็จ
- [ ] ทดสอบส่งข้อความได้ (ใช้ Groq หรือ Ollama)
- [ ] ข้อความบันทึกใน Database
- [ ] Clear Chat ทำงานได้

---

## 📦 วิธี Zip โปรเจคเพื่อส่ง

### ไฟล์ที่ต้อง Zip

```
mini-chat-ollama/
├── backend/
├── frontend/
├── README.md
├── README.th.md
├── SETUP_GUIDE.md (ไฟล์นี้)
├── QUICKSTART.md
├── PROJECT_SUMMARY.md
├── CONTRIBUTING.md
├── CHANGELOG.md
├── LICENSE
├── docker-compose.yml
├── setup.sh
├── .gitignore
└── package.json
```

### ไฟล์ที่ไม่ต้อง Zip (ลบก่อน Zip)

```
❌ backend/node_modules/
❌ frontend/node_modules/
❌ frontend/.next/
❌ backend/.env (มีข้อมูลส่วนตัว)
❌ frontend/.env.local (มีข้อมูลส่วนตัว)
```

### คำสั่งลบไฟล์ที่ไม่ต้องการ (Windows)

```bash
# ลบ node_modules
rmdir /s /q backend\node_modules
rmdir /s /q frontend\node_modules

# ลบ .next
rmdir /s /q frontend\.next

# ลบไฟล์ .env (ถ้ามี API Key)
del backend\.env
del frontend\.env.local
```

**หมายเหตุ:** ไฟล์ `.env.example` และ `.env.local.example` **ต้องเก็บไว้** เพราะเป็นตัวอย่างสำหรับคนที่รับโปรเจค

---

## 🎯 คำแนะนำสำหรับผู้รับโปรเจค

เมื่อได้รับไฟล์ zip แล้ว:

1. **แตก zip** ลงในโฟลเดอร์
2. **อ่านไฟล์นี้** (SETUP_GUIDE.md) ทั้งหมด
3. **ติดตั้งโปรแกรมพื้นฐาน** (Node.js, MongoDB)
4. **เลือกใช้ AI** (Ollama หรือ Groq)
5. **ติดตั้ง dependencies** (`npm install`)
6. **สร้างไฟล์ .env** (copy จาก .env.example)
7. **รันโปรเจค** (backend + frontend)
8. **เปิดเว็บ** http://localhost:3000

---

## 💡 เคล็ดลับ

### สำหรับการนำเสนอ

- ✅ ใช้ **Groq** (ตอบเร็ว ประทับใจ)
- ✅ เตรียม API Key ไว้ล่วงหน้า
- ✅ ทดสอบก่อนนำเสนอ

### สำหรับการพัฒนาต่อ

- ✅ ใช้ **Ollama** (ฟรี ไม่มี limit)
- ✅ เพิ่มฟีเจอร์ใหม่ได้ตามต้องการ
- ✅ อ่าน CONTRIBUTING.md

### สำหรับการใช้งานจริง

- ✅ ใช้ **Groq** หรือ **OpenRouter**
- ✅ ตั้งค่า rate limiting
- ✅ เพิ่ม authentication

---

## 📞 ติดปัญหา?

1. อ่าน [แก้ปัญหาที่พบบ่อย](#แก้ปัญหาที่พบบ่อย) ด้านบน
2. ดู README.md และ README.th.md
3. ตรวจสอบ Console ใน Browser (F12)
4. ดู Terminal ที่รัน Backend/Frontend

---

## ✅ สรุป

โปรเจคนี้พร้อมใช้งาน 100% โดย:

- ✅ **ติดตั้งง่าย** - ทำตามขั้นตอนได้เลย
- ✅ **ใช้ฟรีได้** - ใช้ Ollama หรือ Groq
- ✅ **เอกสารครบ** - มี 7 ไฟล์เอกสาร
- ✅ **พร้อมส่ง** - Zip แล้วส่งได้เลย

**ขอให้โชคดีกับการนำเสนอ! 🎉**

---

**สร้างโดย:** Antigravity AI  
**วันที่:** 9 ธันวาคม 2024  
**เวอร์ชัน:** 1.0.0
