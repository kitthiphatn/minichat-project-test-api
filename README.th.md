# 🤖 Mini Chat Ollama - คู่มือภาษาไทย

แอปพลิเคชันแชทกับ AI ที่รองรับหลายผู้ให้บริการ (Ollama, OpenRouter, Groq, Anthropic)

![Version](https://img.shields.io/badge/version-1.0.0-blue.svg)
![License](https://img.shields.io/badge/license-MIT-green.svg)

---

## 🚀 เริ่มต้นใช้งานเร็ว

**ต้องการติดตั้งและใช้งานทันที?** → อ่าน **[SETUP_GUIDE.md](SETUP_GUIDE.md)** (คู่มือฉบับสมบูรณ์)

**2 วิธีใช้ AI:**
1. ✅ **Ollama** - ฟรี 100%, รันบนเครื่อง (ต้องติดตั้ง)
2. ✅ **Groq** - ฟรี, ตอบเร็ว, ไม่ต้องติดตั้ง (ต้องสมัคร API Key)

---

## 📋 สารบัญ

- [แอปนี้คืออะไร](#แอปนี้คืออะไร)
- [ทำอะไรได้บ้าง](#ทำอะไรได้บ้าง)
- [เทคโนโลยีที่ใช้](#เทคโนโลยีที่ใช้)
- [สิ่งที่ต้องเตรียม](#สิ่งที่ต้องเตรียม)
- [วิธีติดตั้ง](#วิธีติดตั้ง)
- [วิธีใช้งาน](#วิธีใช้งาน)
- [การตั้งค่า](#การตั้งค่า)
- [แก้ปัญหาเบื้องต้น](#แก้ปัญหาเบื้องต้น)
- [คำถามที่พบบ่อย](#คำถามที่พบบ่อย)

---

## แอปนี้คืออะไร

**Mini Chat Ollama** เป็นเว็บแอปพลิเคชันสำหรับแชทกับ AI โดยคุณสามารถ:
- 💬 คุยกับ AI ได้เหมือนแชทกับเพื่อน
- 🔄 เปลี่ยนระหว่าง AI 4 ตัวได้ตามต้องการ
- 💾 บันทึกประวัติการสนทนาไว้ใน Database
- 🎨 ใช้งานง่าย หน้าตาสวยงาม

### ทำไมต้องใช้แอปนี้?

- ✅ **ใช้ฟรี** - ถ้าใช้ Ollama (รันบนเครื่องตัวเอง)
- ✅ **ปลอดภัย** - ข้อมูลอยู่ในเครื่องคุณ
- ✅ **หลายตัวเลือก** - เลือกใช้ AI ได้ 4 ผู้ให้บริการ
- ✅ **ใช้ง่าย** - เปิดเว็บแล้วพิมพ์ได้เลย

---

## ทำอะไรได้บ้าง

### ฟีเจอร์หลัก

1. **แชทกับ AI**
   - พิมพ์ข้อความถาม AI จะตอบกลับมา
   - ข้อความยาวได้สูงสุด 500 ตัวอักษร
   - มีตัวนับตัวอักษรให้ดู

2. **เลือก AI ได้หลายแบบ**
   - **Ollama** - AI ที่รันบนเครื่องคุณ (ฟรี)
   - **OpenRouter** - AI หลายรุ่นจากคลาวด์
   - **Groq** - AI ที่ตอบเร็วมาก
   - **Anthropic** - AI Claude ที่ตอบดีมาก

3. **บันทึกประวัติ**
   - ข้อความทั้งหมดบันทึกใน MongoDB
   - ปิดเว็บแล้วเปิดใหม่ ข้อความยังอยู่
   - กดปุ่มล้างได้ถ้าต้องการเริ่มใหม่

4. **ใช้งานสะดวก**
   - หน้าจอเลื่อนลงอัตโนมัติเมื่อมีข้อความใหม่
   - แสดงสถานะกำลังโหลดขณะ AI กำลังคิด
   - แสดงเวลาที่ AI ใช้ตอบ

---

## เทคโนโลยีที่ใช้

### ส่วนหลังบ้าน (Backend)
- **Node.js** - โปรแกรมรันเซิร์ฟเวอร์
- **Express.js** - เฟรมเวิร์กสำหรับทำ API
- **MongoDB** - ฐานข้อมูลเก็บข้อความ

### ส่วนหน้าบ้าน (Frontend)
- **Next.js 14** - เฟรมเวิร์ก React สำหรับทำเว็บ
- **Tailwind CSS** - ตกแต่งหน้าตา

### AI ที่รองรับ
1. **Ollama** - รันบนเครื่องตัวเอง (แนะนำ)
2. **OpenRouter** - ใช้ผ่านอินเทอร์เน็ต
3. **Groq** - ตอบเร็ว
4. **Anthropic** - Claude AI

---

## สิ่งที่ต้องเตรียม

### จำเป็นต้องมี

1. **Node.js** เวอร์ชัน 18 ขึ้นไป
   - ดาวน์โหลดที่: https://nodejs.org/
   - ตรวจสอบว่าติดตั้งแล้ว: เปิด Command Prompt พิมพ์ `node --version`

2. **MongoDB** (เลือกอย่างใดอย่างหนึ่ง)
   - **แบบง่าย**: ใช้ Docker (แนะนำ)
   - **แบบปกติ**: ติดตั้ง MongoDB ลงเครื่อง

3. **Ollama** (สำหรับใช้ AI ฟรี)
   - ดาวน์โหลดที่: https://ollama.ai/
   - ติดตั้งแล้วพิมพ์ `ollama --version` เช็คได้

### ไม่จำเป็น (แต่มีดีกว่า)

- **Docker** - สำหรับรัน MongoDB แบบง่าย
- **API Key** - ถ้าจะใช้ AI แบบคลาวด์ (OpenRouter, Groq, Anthropic)

---

## วิธีติดตั้ง

### ขั้นตอนที่ 1: ติดตั้งโปรแกรมที่จำเป็น

#### ติดตั้ง Node.js
1. ไปที่ https://nodejs.org/
2. ดาวน์โหลดเวอร์ชัน LTS (แนะนำ)
3. ติดตั้งตามขั้นตอน (กด Next ไปเรื่อยๆ)
4. เปิด Command Prompt พิมพ์ `node --version` ต้องเห็นเลขเวอร์ชัน

#### ติดตั้ง MongoDB (เลือกวิธีใดวิธีหนึ่ง)

**วิธีที่ 1: ใช้ Docker (ง่ายที่สุด)**
1. ติดตั้ง Docker Desktop จาก https://www.docker.com/
2. เปิด Docker Desktop ให้รันอยู่
3. เปิด Command Prompt ในโฟลเดอร์โปรเจค
4. พิมพ์: `docker-compose up -d`
5. เสร็จ! MongoDB รันแล้ว

**วิธีที่ 2: ติดตั้งตรงๆ**
1. ไปที่ https://www.mongodb.com/try/download/community
2. ดาวน์โหลดและติดตั้ง
3. รัน MongoDB Service

#### ติดตั้ง Ollama
1. ไปที่ https://ollama.ai/
2. ดาวน์โหลดตามระบบปฏิบัติการ (Windows/Mac/Linux)
3. ติดตั้งตามขั้นตอน
4. เปิด Command Prompt พิมพ์:
   ```bash
   ollama serve
   ```
5. เปิด Command Prompt อีกหน้าต่าง พิมพ์:
   ```bash
   ollama pull llama3
   ```
   (รอดาวน์โหลด AI model ประมาณ 4-5 GB)

### ขั้นตอนที่ 2: ติดตั้งโปรเจค

1. **เปิด Command Prompt** ในโฟลเดอร์ `mini-chat-ollama`

2. **ติดตั้ง Backend**
   ```bash
   cd backend
   npm install
   ```
   (รอสักครู่ให้โหลดไฟล์เสร็จ)

3. **ติดตั้ง Frontend**
   ```bash
   cd ..\frontend
   npm install
   ```
   (รอสักครู่ให้โหลดไฟล์เสร็จ)

4. **ตั้งค่าไฟล์ Environment**
   
   **สำหรับ Backend:**
   - ไปที่โฟลเดอร์ `backend`
   - คัดลอกไฟล์ `.env.example` เป็น `.env`
   - เปิดไฟล์ `.env` ด้วย Notepad
   - ตรวจสอบว่ามีค่าเหล่านี้:
     ```
     PORT=5000
     MONGODB_URI=mongodb://localhost:27017/mini-chat-ollama
     OLLAMA_BASE_URL=http://localhost:11434
     CORS_ORIGIN=http://localhost:3000
     ```

   **สำหรับ Frontend:**
   - ไปที่โฟลเดอร์ `frontend`
   - คัดลอกไฟล์ `.env.local.example` เป็น `.env.local`
   - เปิดไฟล์ `.env.local` ด้วย Notepad
   - ตรวจสอบว่ามี:
     ```
     NEXT_PUBLIC_API_URL=http://localhost:5000/api
     ```

---

## วิธีใช้งาน

### เริ่มใช้งานครั้งแรก

1. **เปิด MongoDB**
   ```bash
   # ถ้าใช้ Docker
   docker-compose up -d
   
   # ถ้าติดตั้งตรง ให้เปิด MongoDB Service
   ```

2. **เปิด Ollama**
   - เปิด Command Prompt หน้าต่างที่ 1
   - พิมพ์: `ollama serve`
   - ปล่อยไว้รันต่อ

3. **เปิด Backend**
   - เปิด Command Prompt หน้าต่างที่ 2
   - พิมพ์:
     ```bash
     cd backend
     npm run dev
     ```
   - ต้องเห็นข้อความ "Server running on http://localhost:5000"

4. **เปิด Frontend**
   - เปิด Command Prompt หน้าต่างที่ 3
   - พิมพ์:
     ```bash
     cd frontend
     npm run dev
     ```
   - ต้องเห็นข้อความ "Ready on http://localhost:3000"

5. **เปิดเว็บเบราว์เซอร์**
   - เปิด Chrome, Edge, หรือ Firefox
   - ไปที่: **http://localhost:3000**
   - จะเห็นหน้าแชท!

### วิธีใช้งานหน้าแชท

1. **เลือก AI Provider**
   - คลิกที่ dropdown "Provider"
   - เลือก "Ollama (Local)" (ฟรี)

2. **เลือก Model**
   - คลิกที่ dropdown "Model"
   - เลือก "llama3" (แนะนำ)

3. **พิมพ์ข้อความ**
   - พิมพ์คำถามในช่องด้านล่าง
   - เช่น: "สวัสดี คุณเป็นใครครับ?"
   - กด Enter หรือคลิก "Send"

4. **รอ AI ตอบ**
   - จะเห็นจุดกระพริบ "กำลังคิด..."
   - รอประมาณ 5-10 วินาที
   - AI จะตอบกลับมา

5. **ดูประวัติ**
   - ข้อความทั้งหมดแสดงบนหน้าจอ
   - ข้อความของคุณ: สีน้ำเงิน ทางขวา
   - ข้อความของ AI: สีเทา ทางซ้าย

6. **ล้างการสนทนา**
   - คลิกปุ่ม "Clear Chat" ด้านบน
   - ยืนยันว่าต้องการลบ
   - ข้อความทั้งหมดจะหายไป

---

## การตั้งค่า

### ใช้ AI แบบคลาวด์ (ต้องมี API Key)

ถ้าต้องการใช้ AI แบบอื่นนอกจาก Ollama:

#### OpenRouter
1. ไปที่ https://openrouter.ai/
2. สมัครสมาชิกและเติมเงิน
3. คัดลอก API Key
4. เปิดไฟล์ `backend/.env`
5. เพิ่มบรรทัด:
   ```
   OPENROUTER_API_KEY=sk-or-xxxxxxxxxxxxxxxx
   ```
6. รีสตาร์ท Backend (กด Ctrl+C แล้วรันใหม่)

#### Groq
1. ไปที่ https://console.groq.com/
2. สมัครสมาชิก (ฟรี!)
3. คัดลอก API Key
4. เปิดไฟล์ `backend/.env`
5. เพิ่มบรรทัด:
   ```
   GROQ_API_KEY=gsk_xxxxxxxxxxxxxxxx
   ```
6. รีสตาร์ท Backend

#### Anthropic (Claude)
1. ไปที่ https://console.anthropic.com/
2. สมัครสมาชิกและเติมเงิน
3. คัดลอก API Key
4. เปิดไฟล์ `backend/.env`
5. เพิ่มบรรทัด:
   ```
   ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxx
   ```
6. รีสตาร์ท Backend

---

## แก้ปัญหาเบื้องต้น

### ปัญหา: MongoDB ไม่เชื่อมต่อ

**อาการ:** ขึ้นข้อความ "MongoDB connection failed"

**วิธีแก้:**
1. ตรวจสอบว่า MongoDB รันอยู่หรือไม่
   ```bash
   # ถ้าใช้ Docker
   docker ps
   # ต้องเห็น container ชื่อ mini-chat-mongodb
   
   # ถ้าไม่เห็น ให้รัน
   docker-compose up -d
   ```

2. ตรวจสอบไฟล์ `backend/.env`
   ```
   MONGODB_URI=mongodb://localhost:27017/mini-chat-ollama
   ```

### ปัญหา: Ollama ไม่ตอบ

**อาการ:** ขึ้นข้อความ "Ollama connection failed"

**วิธีแก้:**
1. ตรวจสอบว่า Ollama รันอยู่หรือไม่
   ```bash
   # เปิด Command Prompt ใหม่
   ollama serve
   ```

2. ตรวจสอบว่าดาวน์โหลด model แล้ว
   ```bash
   ollama list
   # ต้องเห็น llama3 ในรายการ
   
   # ถ้าไม่มี ให้ดาวน์โหลด
   ollama pull llama3
   ```

### ปัญหา: Frontend เชื่อมต่อ Backend ไม่ได้

**อาการ:** ขึ้นข้อความ "Network Error" หรือ "Failed to fetch"

**วิธีแก้:**
1. ตรวจสอบว่า Backend รันอยู่
   - เปิด http://localhost:5000/health
   - ต้องเห็น `{"status":"OK"}`

2. ตรวจสอบไฟล์ `frontend/.env.local`
   ```
   NEXT_PUBLIC_API_URL=http://localhost:5000/api
   ```

3. รีสตาร์ททั้ง Backend และ Frontend

### ปัญหา: Port ถูกใช้งานอยู่แล้ว

**อาการ:** ขึ้นข้อความ "Port 5000 is already in use"

**วิธีแก้:**

**Windows:**
```bash
# หา process ที่ใช้ port 5000
netstat -ano | findstr :5000

# จดเลข PID (ตัวเลขท้ายสุด)
# ปิด process
taskkill /PID <เลข PID> /F
```

**หรือเปลี่ยน Port:**
1. เปิด `backend/.env`
2. เปลี่ยน `PORT=5000` เป็น `PORT=5001`
3. เปิด `frontend/.env.local`
4. เปลี่ยนเป็น `NEXT_PUBLIC_API_URL=http://localhost:5001/api`
5. รีสตาร์ททั้งสอง

---

## คำถามที่พบบ่อย

### ถาม: ต้องเสียเงินไหม?
**ตอบ:** ไม่ต้อง! ถ้าใช้ Ollama (รันบนเครื่องตัวเอง) ใช้ฟรี 100%
แต่ถ้าจะใช้ AI แบบคลาวด์ (OpenRouter, Groq, Anthropic) ต้องเสียเงินตามการใช้งาน

### ถาม: ต้องมีอินเทอร์เน็ตไหม?
**ตอบ:** 
- ใช้ Ollama: **ไม่ต้อง** (หลังจากดาวน์โหลด model แล้ว)
- ใช้ AI คลาวด์: **ต้องมี**

### ถาม: เครื่องต้องแรงแค่ไหน?
**ตอบ:**
- **CPU:** ธรรมดาก็ได้ (แต่มี GPU จะเร็วกว่า)
- **RAM:** อย่างน้อย 8 GB (แนะนำ 16 GB)
- **Storage:** อย่างน้อย 10 GB (สำหรับ AI model)

### ถาม: AI ตอบภาษาไทยได้ไหม?
**ตอบ:** ได้! ทุก AI ที่รองรับเข้าใจและตอบภาษาไทยได้
แต่ llama3 อาจจะตอบภาษาไทยไม่ชัดเท่า Claude หรือ GPT

### ถาม: ข้อมูลปลอดภัยไหม?
**ตอบ:** 
- ใช้ Ollama: **ปลอดภัยมาก** ข้อมูลไม่ออกจากเครื่อง
- ใช้ AI คลาวด์: ข้อมูลส่งไปยังเซิร์ฟเวอร์ของผู้ให้บริการ

### ถาม: ลบข้อความแล้วกู้คืนได้ไหม?
**ตอบ:** ไม่ได้ เมื่อกด "Clear Chat" แล้วข้อความจะถูกลบถาวร

### ถาม: ใช้พร้อมกันหลายคนได้ไหม?
**ตอบ:** ได้ แต่ตอนนี้ยังไม่มีระบบ login ข้อความจะรวมกัน
ถ้าต้องการแยกผู้ใช้ ต้องพัฒนาเพิ่ม

### ถาม: AI model ไหนดีที่สุด?
**ตอบ:**
- **llama3** - ฟรี, ดี, เร็วพอใช้
- **claude-3-sonnet** - ตอบดีมาก แต่เสียเงิน
- **groq/llama3** - เร็วมาก แต่ต้องมี API key
- **gemini-pro** - สมดุลระหว่างคุณภาพกับราคา

---

## 🎯 เริ่มต้นใช้งานเลย!

### ขั้นตอนย่อ (สำหรับคนรีบ)

```bash
# 1. ติดตั้ง dependencies
cd backend && npm install
cd ../frontend && npm install

# 2. คัดลอกไฟล์ .env
cd backend && copy .env.example .env
cd ../frontend && copy .env.local.example .env.local

# 3. เปิด MongoDB
docker-compose up -d

# 4. เปิด Ollama (Command Prompt หน้าต่างที่ 1)
ollama serve

# 5. ดาวน์โหลด AI model (Command Prompt หน้าต่างที่ 2)
ollama pull llama3

# 6. เปิด Backend (Command Prompt หน้าต่างที่ 3)
cd backend
npm run dev

# 7. เปิด Frontend (Command Prompt หน้าต่างที่ 4)
cd frontend
npm run dev

# 8. เปิดเว็บ
# http://localhost:3000
```

---

## 📚 เอกสารเพิ่มเติม

- **[README.md](README.md)** - เอกสารภาษาอังกฤษฉบับเต็ม
- **[QUICKSTART.md](QUICKSTART.md)** - คู่มือเริ่มต้นใช้งานเร็ว
- **[PROJECT_SUMMARY.md](PROJECT_SUMMARY.md)** - สรุปโปรเจค

---

## 💡 เคล็ดลับการใช้งาน

1. **ประหยัด RAM:** ใช้ model เล็กๆ เช่น `phi3` แทน `llama3:70b`
2. **ตอบเร็วขึ้น:** ใช้ Groq API (ฟรี แต่มี limit)
3. **คุณภาพดี:** ใช้ Claude 3 Sonnet (เสียเงิน)
4. **ฟรีที่สุด:** ใช้ Ollama + llama3

---

## 🆘 ติดปัญหาหรือต้องการความช่วยเหลือ?

1. อ่าน [แก้ปัญหาเบื้องต้น](#แก้ปัญหาเบื้องต้น) ด้านบน
2. ดูเอกสาร Troubleshooting ใน README.md
3. ตรวจสอบว่าติดตั้งโปรแกรมครบหรือไม่
4. ลองรีสตาร์ททุกอย่างใหม่

---

**สร้างด้วย ❤️ สำหรับคนไทย | ธันวาคม 2024**

**ขอให้สนุกกับการแชทกับ AI! 🎉**
